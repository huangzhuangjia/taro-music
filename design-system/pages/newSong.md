# 页面覆盖 · 最新单曲 NewSong

## 列表增强（可选）

- 每行前增加序号 `01`–`99`：宽 `56rpx`，色 `$jm-color-text-tertiary`，`$jm-font-caption`
- 播放中序号改 `$jm-color-primary`

## 行样式

- 统一 **Media Row**
- `.song-itembox-active`：背景 `$jm-color-primary-soft`，右侧播放图标 `$jm-color-primary`（替代 `#666`）

## 隐藏块

- `.newest-banner { display: none }` 保持，不占高度
