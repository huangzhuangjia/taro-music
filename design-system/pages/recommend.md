# 页面覆盖 · 推荐歌单 Recommend

## Hero

- `.recommend-banner`：`padding: $jm-space-md $jm-page-x 0`
- `.play-btn`：主色实心按钮规范（见 MASTER 3.2）
- `.name`：`$jm-font-display`，最多 2 行，`line-clamp`（小程序用 `-webkit-line-clamp: 2`）
- `.desc`：最多 2 行，`$jm-color-text-secondary`

## 列表

- `.album-itembox` → 对齐 **Media Row**，封面 `96rpx`
- 播放量行：icon + 数字，`$jm-font-caption`，图标色 `$jm-color-text-secondary`

## 滚动

- `.item-list`：`padding-bottom: $jm-list-bottom`
